#identitymatrix.py
"AUTHOR---->MUDITH NAHATA
import numpy as np
a=np.eye(4)
print("{}".format(a))
#output the matrix will be printed with the diagonal 1 with the 4 rows and 4 columns

#creation of numpy array wih random values
b=np.random.random((3,4))
print("{}".format(b))
# output->all the values will be from zero or one with a random floating point values of b array

#creation of random integer values with a specific range
b=np.random.randint(10,100(3,5))
print("{}".format(b))
#output->the 10,100 is the range of specific value and three specifies rows and four specifies columns
